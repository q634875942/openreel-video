{
  "project": "openreel-video fork — AI-generated objects for self-media video creation",
  "upstream": "https://github.com/Augani/openreel-video",
  "fork": "https://github.com/q634875942/openreel-video",
  "features": [
    {
      "id": "feat-000",
      "name": "Slice 0 — Repo bootstrap and architecture exploration",
      "description": "Fork upstream, set up dev environment (Node, pnpm, Vite), map monorepo structure, locate Three.js integration point, identify GeneratedClip plug-in surface.",
      "dependencies": [],
      "status": "done",
      "evidence": "See SLICE0_FINDINGS.md. Dev server verified at http://localhost:5173 (HTTP 200). Committed in c55a542."
    },
    {
      "id": "feat-001",
      "name": "Slice 1.1 — GeneratedClip type definition",
      "description": "Add `GeneratedClip` to `packages/core/src/graphics/types.ts` mirroring ShapeClip/SVGClip shape. Fields: source (TS string), sourceLanguage, providerId, promptHistory, paramsSchema, params, transform, keyframes, blendMode, blendOpacity. Re-export from @openreel/core.",
      "dependencies": ["feat-000"],
      "status": "done",
      "evidence": "Extended GraphicType union with 'generated'; added GeneratedClip interface, GeneratedClipPromptMessage, GeneratedClipSourceLanguage type alias, and DEFAULT_GENERATED_PARAMS_SCHEMA constant. Auto-exported via existing `graphics/index.ts` → `core/src/index.ts` chain (no manual re-export needed). Verified: `pnpm typecheck` clean across all 5 workspace projects (apps/web, apps/image, packages/core, packages/ui, packages/image-core); `pnpm --filter @openreel/core test:run` 176/176 tests pass in 1.22s, including 50 graphics-engine tests."
    },
    {
      "id": "feat-002",
      "name": "Slice 1.2 — GeneratedClip renderer integration",
      "description": "Extend `ThreeJSLayerRenderer` with `renderGeneratedClip(clip, time)` or create sibling `GeneratedClipRenderer` sharing the same Scene/Camera. Hook into `canvas-renderers.ts` dispatch. For Slice 1 MVP, support a hard-coded test clip (no AI yet) that draws something visible.",
      "dependencies": ["feat-001"],
      "status": "done",
      "evidence": "Added `renderGeneratedClip` to ThreeJSLayerRenderer (Three.js / WebGL path) and `renderGeneratedClipOnly` (Canvas 2D fast path) — both draw a colored rectangle whose fill comes from `params.color` (default `#3b82f6`). Source string is intentionally ignored at this layer (sandbox lands in feat-003). Exposed `readGeneratedClipColor` + `DEFAULT_GENERATED_CLIP_COLOR` from threejs-layer-renderer. Extended `GraphicClipUnion` and added `generated` branches in both the 3D dispatch (line ~1540) and 2D dispatch (line ~1606) inside `renderShapeClipToCanvas`. Widened `ShapeClip | SVGClip | StickerClip` literals in Preview.tsx to include GeneratedClip (`getGraphicClipDisplayBounds`, `findGraphicClipAtPoint`, and 5 other call sites). Added 5 unit tests for `readGeneratedClipColor` in `canvas-renderers.test.ts`. Verification: workspace `pnpm typecheck` clean across all 5 projects; `pnpm --filter @openreel/web test:run` 125 passed / 7 skipped (5 new tests for feat-002 all green); the single failing test (`project-store.test.ts:851` `editing templates` track ordering) is pre-existing on upstream main — verified by git-stashing feat-002 changes and reproducing the same failure (120 passed baseline -> 125 passed with feat-002 = exactly +5 new)."
    },
    {
      "id": "feat-003",
      "name": "Slice 1.3 — Sandbox execution layer",
      "description": "Create `apps/web/src/objects/Sandbox.ts`. Execute generated TS source inside a Web Worker. Expose only a procedural SceneDescription protocol (rect/circle/line/text). Define message protocol: init(source) → init-ok|init-error, frame(t, params, requestId) → frame-result|frame-error.",
      "dependencies": ["feat-001"],
      "status": "done",
      "evidence": "Chose Web Worker + procedural SceneDescription over `new Function` with restricted globals. Reasoning: hang-isolation is the only decisive advantage of either approach for our single-user threat model, and procedural output keeps the Worker complexity much lower than passing Three.js objects across the boundary. Files: `apps/web/src/objects/SceneDescription.ts` (Shape union: rect/circle/line/text, normalized 0..1 coords, EMPTY_SCENE), `sandbox-engine.ts` (pure compileSource + runFrame returning discriminated-union Results, no Worker dependency — fully testable in jsdom), `sandbox-protocol.ts` (postMessage wire types), `sandbox-worker.ts` (thin Worker shell wiring engine to self.onmessage), `Sandbox.ts` (main-thread wrapper: init/renderFrame/getLatestScene/dispose, configurable per-call timeout, requestId multiplexing, Worker factory injection for tests), `index.ts` (barrel export). Verification: workspace `pnpm typecheck` clean across all 5 projects; `pnpm --filter @openreel/web test:run` 147 passed / 7 skipped (vs 125 baseline) = +22 new tests across sandbox-engine.test.ts (14 cases covering compile success/empty/whitespace/syntax-error/non-object/null/missing-frame, runFrame with t/params, thrown errors, malformed scenes) and Sandbox.test.ts (8 cases covering init resolve/reject, frame happy path, getLatestScene, concurrent multiplexing, engine errors surfacing, frame timeout, dispose-rejects-inflight, use-after-dispose); same pre-existing `project-store.test.ts:851` failure as feat-002. AI source contract: an expression evaluating to `{ frame(t, params): SceneDescription }`. No `import` or top-level function declarations allowed (single expression body required by `new Function` wrapping in sandbox-engine)."
    },
    {
      "id": "feat-004",
      "name": "Slice 2.1 — AIProvider abstraction",
      "description": "Define `AIProvider` interface in `apps/web/src/ai/AIProvider.ts`. Methods: generate(request) → AsyncIterable<chunk>, listModels(). Common request shape (system/messages/tools/stream/temperature). Tools use JSON Schema as the lowest common denominator across providers.",
      "dependencies": ["feat-000"],
      "status": "not-started",
      "evidence": ""
    },
    {
      "id": "feat-005",
      "name": "Slice 2.2 — Provider implementations (Claude / OpenAI / DeepSeek / Compatible)",
      "description": "Implement ClaudeProvider (anthropic SDK + prompt caching), OpenAIProvider (openai SDK), DeepSeekProvider (openai SDK + baseURL override), CompatibleProvider (user supplies baseURL/key/model). Provider registry + persistence of current selection in IndexedDB. Errors are surfaced — no silent fallback.",
      "dependencies": ["feat-004"],
      "status": "not-started",
      "evidence": ""
    },
    {
      "id": "feat-006",
      "name": "Slice 2.3 — AI Panel UI",
      "description": "Create `apps/web/src/components/AIPanel/`. Provider+model dropdown, prompt input, generate button, streaming output area, 'Fix with AI' on render error. Settings entry for API keys (stored encrypted in IndexedDB via `keyStore.ts`). Wire createGeneratedClip into project store.",
      "dependencies": ["feat-002", "feat-005"],
      "status": "not-started",
      "evidence": ""
    },
    {
      "id": "feat-007",
      "name": "Slice 3 — Visual parameter editing (JsonSchema → form)",
      "description": "Component `apps/web/src/components/ParamPanel/` reads a GeneratedClip's `paramsSchema` and renders inputs (color picker, number slider, vector, select, text). Param changes flow back to clip without re-running AI. Params support keyframe binding via the existing AnimationEngine.",
      "dependencies": ["feat-006"],
      "status": "not-started",
      "evidence": ""
    },
    {
      "id": "feat-008",
      "name": "Slice 4 — Monaco source editor",
      "description": "Component `apps/web/src/components/SourceEditor/` opens on double-click of a GeneratedClip and shows its `source`. Save re-mounts the clip's sandbox with new source. 'Ask AI to refactor' button: send selection + instruction to provider, replace selection with response.",
      "dependencies": ["feat-006"],
      "status": "not-started",
      "evidence": ""
    },
    {
      "id": "feat-009",
      "name": "Slice 5 — Action/state system",
      "description": "Allow each GeneratedClip to define named states (idle/walk/jump etc.) as parameter presets. Timeline can place 'state change' markers that trigger smooth transitions of params between states. Built on top of existing keyframe machinery, no new core engine.",
      "dependencies": ["feat-007"],
      "status": "not-started",
      "evidence": ""
    },
    {
      "id": "feat-010",
      "name": "Slice 6 — Object template library + E2E verification",
      "description": "Bundled object templates (stick figure, geometric shapes, text bubble, emoji character). User can pick a template and ask AI to mutate it. End-to-end smoke: prompt → generate → add to timeline → tweak params → export mp4, with playback matching preview at 1080p.",
      "dependencies": ["feat-008", "feat-009"],
      "status": "not-started",
      "evidence": ""
    }
  ],
  "policies": {
    "one_feature_at_a_time": true,
    "verification_required_for_done": true,
    "additive_only": "New code lives in src/ai/, src/objects/, src/components/AIPanel|ParamPanel|SourceEditor/. Existing openreel files only modified if the feature strictly requires it.",
    "rebase_target": "upstream/main"
  }
}
